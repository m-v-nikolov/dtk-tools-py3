[EXAMPLE]
type = LOCAL
input_root = C:\Projects\dtk-tools\examples\inputs

[HPC2]
type = HPC
server_endpoint = https://comps2.idmod.org
environment = Bayesian
input_root = $COMPS_PATH(USER)\input\
sims_per_thread = 20

[HPC]
type = HPC
priority = Lowest
server_endpoint = https://comps2.idmod.org
lib_staging_root = $COMPS_PATH(HOME)\braybaud\malariaongoing
bin_staging_root = $COMPS_PATH(HOME)\braybaud\malariaongoing\Eradication.exe

[SSMT]
type = HPC
bin_staging_root. = $COMPS_PATH(USER)\bin\model.py
bin_staging_root = C:\Python27\python.exe
environment = UCIcloud
user = braybaud

